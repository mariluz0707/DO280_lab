apiVersion: v1
kind: Service
metadata:
  name: stock
  namespace: network-review
  annotations:
    CHANGE_ME
spec:
  selector:
    app: stock
  ports:
    - port: 80
      targetPort: 8085
      name: http
